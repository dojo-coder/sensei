-- TODO: greet the world from the database.
--   1. Create a 'greetings' table with columns id (INTEGER PRIMARY KEY) and
--      message (TEXT NOT NULL).
--   2. Insert the row (1, 'Hello, World!').
--   3. SELECT message FROM greetings so it shows up in the Preview panel.

-- 1. CREATE TABLE greetings ...
-- 2. INSERT INTO greetings ...
-- 3. SELECT message FROM greetings;
